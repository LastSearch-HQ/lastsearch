# Smithery configuration file: https://smithery.ai/docs/deployments

build:
  dockerBuildPath: ./apps/mcp
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      browseApiKey:
        type: string
        description: LastSearch API key (get one at https://lastsearch.dev/dashboard)
  commandFunction:
    |-
    (config) => ({ command: 'node', args: ['dist/index.js'], env: { LASTSEARCH_API_KEY: config.browseApiKey || '' } })
